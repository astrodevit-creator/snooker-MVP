---
description: Full plan → implement → verify pipeline for a new feature or fix, via the ceo-orchestrator.
---

Run the complete build pipeline for this request, coordinated by the `ceo-orchestrator`
agent per its defined WORKFLOW (Discovery → Plan → Requirements → Architecture/Database →
Implement → Verify → Fix → Production gate). Do not stop at "implementation complete" —
the task is not done until the DEFINITION OF DONE checklist in `ceo-orchestrator.md` is
satisfied with actual evidence, not assertions.

Request:
$ARGUMENTS
